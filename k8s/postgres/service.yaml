apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: booking
  labels:
    app: postgres
spec:
  type: ClusterIP
  selector:
    app: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP
