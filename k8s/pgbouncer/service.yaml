apiVersion: v1
kind: Service
metadata:
  name: pgbouncer
  namespace: booking
  labels:
    app: pgbouncer
spec:
  type: ClusterIP
  selector:
    app: pgbouncer
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP
